<UserControl x:Class="FitnessTracker.Views.WeightEntryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

  <Grid Margin="20">
    <StackPanel Width="300" HorizontalAlignment="Left">
      <TextBlock Text="Log Weight Entry" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

      <!-- Entry Date -->
      <TextBlock Text="Date:" Margin="0,0,0,5"/>
      <DatePicker SelectedDate="{Binding EntryDate}" Margin="0,0,0,15"/>

      <!-- Weight -->
      <TextBlock Text="Weight (lbs):" Margin="0,0,0,5"/>
      <TextBox Text="{Binding Weight, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>

      <!-- Body Fat Percentage -->
      <TextBlock Text="Body Fat % (optional):" Margin="0,0,0,5"/>
      <TextBox Text="{Binding BodyFatPercentage, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,20"/>

      <!-- Buttons -->
      <StackPanel Orientation="Horizontal">
        <Button Content="Save" Command="{Binding SaveCommand}" Width="80" Margin="0,0,10,0" Padding="10,5"/>
        <Button Content="Clear" Command="{Binding ClearCommand}" Width="80" Padding="10,5"/>
      </StackPanel>
    </StackPanel>
  </Grid>
</UserControl>